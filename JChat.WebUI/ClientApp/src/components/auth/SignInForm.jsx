import React from 'react';
import FullScreen from '@/components/display/FullScreen';

const SignInForm = ({ data: { id, ui }, onSubmit, ...props}) => {

  return (
    <FullScreen $center>

    </FullScreen>
  )
}
